{
  "name": "app",
  "version": "1.0.0",
  "description": "API Backend para o sistema da Paróquia São José - Caraguatatuba (Cloudflare Worker)",
  "type": "module",
  "scripts": {
    "test": "vitest run --dir tests/use-cases",
    "test:watch": "vitest --dir tests/use-cases",
    "dev": "wrangler dev --local",
    "deploy": "wrangler deploy --minify",
    "build": "wrangler build",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "migration:dev": "npx wrangler d1 execute parish_db_dev --local --file migrations/*.sql",
    "lint": "eslint . --ext .ts --fix",
    "format": "prettier --write ."
  },
  "dependencies": {
    "@cloudflare/workers-types": "^4.20250927.0",
    "@hono/swagger-ui": "^0.5.2",
    "hono": "^4.9.9",
    "moment": "^2.30.1",
    "serverless-crypto-utils": "^1.3.0",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "@faker-js/faker": "^10.1.0",
    "@typescript-eslint/eslint-plugin": "^8.46.1",
    "@typescript-eslint/parser": "^8.46.1",
    "eslint": "^9.37.0",
    "prettier": "^3.6.2",
    "vite-tsconfig-paths": "^5.1.4",
    "vitest": "^3.2.4",
    "wrangler": "^4.4.0"
  }
}
